apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rfc-prod

resources:
  - ../base

patches:
  - target:
      group: batch
      version: v1
      kind: Job
      labelSelector: app.kubernetes.io/component=database-migration
    path: rds-ca.patch.yaml
