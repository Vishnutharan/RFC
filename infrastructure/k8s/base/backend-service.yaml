apiVersion: v1
kind: Service
metadata:
  name: rfc-backend
  labels:
    app.kubernetes.io/name: rfc-backend
    app.kubernetes.io/part-of: rfc-watford
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rfc-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
